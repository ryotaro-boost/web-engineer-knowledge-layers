# CLAUDE.md - Web Engineer Knowledge Layers プロジェクト規約

## プロジェクト概要

Webエンジニアリングの知識を8層（Layer 0-7）に分けて体系的に学習・文書化するプロジェクト。Obsidian Vault 内で運用する。

**AIコーディングエージェント時代のエンジニア養成**を目的とし、「コードを書ける」より「**AI出力を批判できる**」「**設計判断を下せる**」「**症状から根本原因へ辿れる**」「**システム全体を俯瞰できる**」という上位の判断力の獲得を主眼に置く。

## 言語規約

- ドキュメントはすべて **日本語** で記述する
- 技術用語は初出時に英語を併記する（例：「計算量（Big-O）」）
- 英語の専門用語はそのままカタカナにせず原語を使う場合がある（例：SOLID, CORS, CDN）

## ディレクトリ構成

```
.
├── _templates/              # トピックテンプレート
├── _starter/                # 学習導線（スターターガイド・最小学習パス・AI協働の基本動作）
├── _graph/                  # レイヤー横断の依存グラフ
├── _anti-patterns/          # AI実装アンチパターン索引（レイヤー別 / パターン別）
├── _glossary/               # 用語集・チートシート（予定）
├── details/                 # 末端知識（個別概念の深掘り、レイヤー横断）
├── Layer{0-7}-*/            # 各レイヤーのトピック
└── Layer{N}-*/_practice/    # 各レイヤーの実践演習（_index + 演習ファイル群）
```

## ファイル命名規約

- フォルダ名: `LayerN-日本語名/` （例: `Layer0-CS基礎/`）
- ファイル名: 日本語トピック名.md（スペースなし、英語略語はハイフン区切り）
- 各レイヤーフォルダに `_index.md` を配置（レイヤー概要 + トピック一覧）

## トピックドキュメントのテンプレート

`_templates/topic-template.md` に従う。テンプレートは AI 協働時代に対応した以下の構造で構成される。

### フロントマター（全項目必須）

| フィールド | 内容 |
|---|---|
| `layer` | レイヤー番号（0-7） |
| `topic` | トピック名 |
| `status` | **🔴 未着手で固定**（学習進捗を機械的に追わない方針） |
| `created` | 作成日 |
| `prerequisites` | 前提となるトピックへの `[[]]` リンクの配列 |
| `next_steps` | 次に学ぶべきトピックへの `[[]]` リンクの配列 |
| `difficulty` | beginner / intermediate / advanced |
| `estimated_minutes` | 通読目安（分） |
| `ai_collaboration` | heavy / partial / minimal（AIに任せやすい度合い） |

### 本文セクション（必須）

1. **一言で言うと** — 1文要約（blockquote）
2. **3分で全体像** — 何を解決する技術か / 使用シーン / これだけ覚える3つ / AIに任せやすいか / 詰まったら開くトピック
3. **なぜ必要か**
4. **どの問題を解決するか**
5. **他の仕組みとどう関係するか** — 下位 / 同 / 上位レイヤーとの関係
6. **誤解されやすいポイント** — 3つ以上
7. **設計のベストプラクティス**
8. **AIエージェントとの協働** — AI協働時代の中核セクション。4サブ節:
   - AIに任せられる部分 / 人間が判断すべき部分（表）
   - AI生成コードのレビュー観点（このトピック固有、3項目）
   - 効くプロンプトの型（前提・やってほしいこと・制約・判断基準のテンプレ）
   - AI実装のアンチパターン（表）
   - `→ [[_anti-patterns/_index|AIアンチパターン索引]]` への横断リンク
9. **具体例** — 主要1言語を本文、他言語は `details/` に切り出す
10. **参考リソース**
11. **理解度セルフチェック** — 3問（**3問目はAI生成コードレビュー設問必須**）。`<details>` で折りたたみ解答を併記

### 任意セクション

- **AI協働演習** — AIに書かせて評価するループを実践する4ステップ
- **学習メモ** — 個人的な気づき・疑問・TODO

## Obsidian リンク規約

- トピック間の参照は `[[トピック名]]` を使用する
- レイヤーをまたぐ参照を積極的に行う（例: `[[インデックス]]` から `[[ファイルシステムとIO]]` へ）
- セキュリティ（Layer 6）とパフォーマンス（Layer 5）は横断的関心事として他レイヤーから頻繁にリンクする

## 学習進捗の記録

- 各 `_index.md` のトピックステータスは **🔴 未着手のまま固定**（学習進捗を機械的に追わない方針）
- 進捗の体感は各トピック内の「理解度セルフチェック」「AI協働演習」と各レイヤー `_practice/` の演習を消化することで得る

## 末端知識ドキュメント（details/）

- プロジェクトルート直下に `details/` ディレクトリを配置する（レイヤー横断で共有するため）
- トピックの個別概念や深掘り情報を格納する（例: `details/ハッシュテーブル.md`）
- フロントマターに `parent: "[[親トピック名]]"` と `type: detail` を含める
- テンプレート構造に厳密に従う必要はないが、概念説明・複数言語のコード例・落とし穴を含めること
- **詳細へのリンクはトピックファイルの本文中にインラインで配置する**（末尾の一覧ではなく、関連するテキスト付近に `[[詳細名]]` を埋め込み、読み進める導線で自然に辿れるようにする）

## 図解規約

- 図解は **Mermaid 記法のみ** を使用する（アスキーアートは禁止）
- 比較表・フロー図・シーケンス図・状態遷移図など、概念の理解を直感的に助けるものを積極的に使う

## 実践演習（_practice/）

- 各レイヤー（特に Layer 3, 4, 5, 6, 7）に `_practice/` サブディレクトリを置く
- 演習は **AI協働6段階** で構成する: お題 → AIへの初期プロンプト設計 → AI生成コードの一次レビュー → 観察ポイントに照らした批判 → 追プロンプトでの修正 → 違反を仕込みAIに直させる
- Layer 7 の `_practice/` を雛形として参照する

## Claude Code スキル

スキルは **`.claude/skills/<name>/SKILL.md`** 形式で配置する（最新ベストプラクティス準拠）。各 SKILL.md はフロントマターに `description`, `argument-hint`, `allowed-tools` を含む。

| コマンド | 役割 | 状態 |
|---------|------|------|
| `/study-topic` | 新テンプレート準拠のトピックドキュメント生成 | 既存 |
| `/create-detail` | 末端知識ドキュメント生成 | 既存 |
| `/review-detail` | 末端知識ドキュメントのレビュー | 既存 |
| `/review-layer` | レイヤー全体のレビュー | 既存 |
| `/explain-3min` | 既存トピックの「3分で全体像」を生成・提案 | 新規（Week 1） |
| `/refactor-topic` | 密度突出節を抽出し `details/` への切り出しを提案 | 新規（Week 1） |
| `/review-ai-code` | AI生成コードを関連トピック観点でレビュー | 新規（Week 1） |
| `/design-prompt` | 機能要件からAIへのプロンプト雛形を生成 | 新規（Week 1） |
| `/quiz` | 動的に理解度クイズを生成（最低2問はAIコードレビュー設問） | 予定（Week 4） |
| `/exercise` | `_practice/` から次の演習を提案 / 雛形生成 | 予定（Week 4） |
| `/diagnose` | 症状から関連レイヤー / トピックへの追跡を案内 | 予定（Week 4） |
| `/anti-pattern-scan` | コードを `_anti-patterns/` 索引と照合 | 予定（Week 4） |
| `/glossary` | 用語追加 / detail リンク張り | 予定（Week 11） |
| `/starter-path` | 最小学習パスを学習者にパーソナライズ | 予定（Week 11） |

## Claude Code への指示

- 新しいトピックドキュメントを作成する際は `_templates/topic-template.md` の構造に従うこと
- `[[リンク]]` は実在するファイル名と一致させること（Glob で実在確認）
- コード例は実行可能な最小限のスニペットとし、言語を明示すること（```python, ```typescript 等）
- 1トピック = 1ファイルの原則を守ること
- 図解は Mermaid 記法を使用すること（アスキーアート禁止）
- 各 `_index.md` のステータスは 🔴 未着手のまま固定運用
- AI協働章は必須セクション。新規トピック作成時に空欄では送らない
- セルフチェック3問目は必ずAI生成コードレビュー設問にする
