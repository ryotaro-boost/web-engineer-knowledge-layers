---
layer: 3
topic: インデックス
type: topic
status: 🔴 未着手
created: 2026-03-28
prerequisites: ["[[RDB]]", "[[データ構造とアルゴリズム]]", "[[計算量-BigO]]", "[[ファイルシステムとIO]]"]
next_steps: ["[[キャッシュ戦略]]", "[[マイグレーション]]", "[[Layer5-パフォーマンス/_index|Layer 5: パフォーマンス]]"]
difficulty: intermediate
estimated_minutes: 50
ai_collaboration: partial
---

# インデックス（Index）

> **一言で言うと:** テーブルの全件走査を回避し、大量データから高速に目的の行を見つけるための補助データ構造。本の「索引」と同じ発想だが、代償として書き込み性能とストレージを消費する。

## 3分で全体像

- **何を解決する技術か:** 100万行・1億行のテーブルから目的の数行を「数回のディスクI/O」で取り出す。フルスキャン O(n) を B+Tree O(log n) に置き換える
- **代表的な使用シーン:** WHERE 句の検索、JOIN の結合キー、ORDER BY のソート、UNIQUE 制約による重複防止、外部キー検証
- **これだけは覚える3つ:**
    1. **インデックスは「読み取りを速くする代償に書き込みを遅くする」トレードオフ** — 「念のため」全カラムに貼ると INSERT/UPDATE が劇的に遅くなる。実際のクエリパターンに基づき必要最小限にする
    2. **複合インデックスは「左端のカラムからしか使えない」** — `(status, created_at)` のインデックスは `status` 単独や `status + created_at` には効くが、`created_at` 単独には効かない（最左プレフィックスルール）
    3. **`EXPLAIN ANALYZE` がインデックス設計の唯一の判定方法** — 「貼ったから速いはず」は思い込み。`Seq Scan` が大きなテーブルに出ていたら設計を見直す
- **AIに任せやすいか:** **一部任せられる** — 単純なクエリのインデックス案を出させるのは AI が得意。ただし「複合インデックスのカラム順」「部分インデックスの条件」「カバリングインデックスの設計」は実クエリ集と書き込み頻度を見て人間が判断すべき。AI は「念のため全カラムに INDEX」を出しがちなので `/review-ai-code` で検出する
- **詰まったらここを読む:** [[RDB]] / [[データ構造とアルゴリズム]] / [[ファイルシステムとIO]]

## なぜ必要か

100万行のテーブルから1行を探すとき、インデックスがなければデータベースは全行を先頭から順に読む（フルテーブルスキャン）。これは [[計算量-BigO]] で言う O(n) である。

具体的にどれほど差が出るかを考えてみる:

| 行数 | フルスキャン（O(n)） | B+Treeインデックス（ページアクセス数） |
|------|-------------------|--------------------------------------|
| 1,000 | 1,000行を走査 | 約2回のページI/O |
| 100万 | 100万行を走査 | 約3回のページI/O |
| 1億 | 1億行を走査 | 約4回のページI/O |

B+Treeは1ノードに数百のキーを格納できるため、ファンアウト（分岐数）が非常に大きく、1億行でもわずか4回のページアクセスで目的の行に到達できる。[[ファイルシステムとIO]]で学んだ通り、ディスクI/Oは極めて遅い操作であるため、アクセス回数の削減はレスポンスタイムに直結する。

インデックスがなければ:
- **ユーザー体験が崩壊する** — 一覧画面の表示に数十秒かかる
- **DB全体が詰まる** — 遅いクエリがCPUとI/Oを占有し、他の全クエリも遅くなる
- **スケーリングが不可能** — データ量の増加に対して性能が線形に劣化する

## どの問題を解決するか

### 1. 検索の高速化 — B-Tree

[[RDB]]で最も広く使われるインデックス構造が **[[B-TreeとB+Tree|B-Tree]]**（Balanced Tree）である。[[データ構造とアルゴリズム]]で学ぶ二分探索木の発展形で、1ノードに複数のキーを持ち、ディスクI/Oに最適化されている。

```mermaid
graph TD
    ROOT["[30 | 60]"]
    N1["[10 | 20]"]
    N2["[40 | 50]"]
    N3["[70 | 80 | 90]"]
    L1["→ rows"]
    L2["→ rows"]
    L3["→ rows"]
    L4["→ rows"]
    L5["→ rows"]
    L6["→ rows"]
    L7["→ rows"]

    ROOT --> N1
    ROOT --> N2
    ROOT --> N3
    N1 --> L1
    N1 --> L2
    N1 --> L3
    N2 --> L4
    N2 --> L5
    N3 --> L6
    N3 --> L7
```

**なぜB-Treeがディスクに最適か:**

ディスクは1バイト読むのも4KBのブロック単位で読むのもほぼ同じ時間がかかる。B-Treeは1ノードを1ブロック（ページ）に対応させ、1回のI/Oで数百のキーを読み込む。結果として、ツリーの高さが極めて低く抑えられる（数百万行でも高さ3-4程度）。

**B-Tree vs B+Tree:**

実際のRDB（PostgreSQL, MySQL InnoDB）が使うのは **B+Tree** である。

| 特性 | B-Tree | B+Tree |
|------|--------|--------|
| データの位置 | 全ノードにデータ | リーフノードのみにデータ |
| リーフノード間のリンク | なし | 双方向リンクリスト |
| 範囲検索 | ツリーを都度走査 | リーフノードを横断するだけ |
| ノードあたりのキー数 | 少ない（データが場所を取る） | 多い（ポインタのみ） |

B+Treeがリーフノードを双方向リンクリストで繋いでいるため、`WHERE age BETWEEN 20 AND 30` のような範囲検索が効率的に行える。

### 2. ソートの高速化

`ORDER BY` はインデックスがなければ全件取得後にメモリ上でソートする（filesort）。インデックスがあれば、既にソート済みのデータをリーフノードの順序で読むだけで済む。

```sql
-- インデックスなし: 全件取得 → メモリでソート → 上位10件を返す
-- インデックスあり: B+Treeの末尾から10件をリーフ順に読むだけ
SELECT * FROM orders ORDER BY created_at DESC LIMIT 10;
```

### 3. 一意性の保証

`UNIQUE` インデックスは検索の高速化に加えて、重複値の挿入を防止する。[[RDB]]の `PRIMARY KEY` や `UNIQUE` 制約は内部的にインデックスを作成している。

### 4. インデックスの種類

| 種類 | 用途 | 例 |
|------|------|-----|
| **B-Tree（デフォルト）** | 等値検索、範囲検索、ソート | `WHERE status = 'active'`、`WHERE age > 20` |
| **Hash** | 等値検索のみ（範囲検索不可） | `WHERE email = 'a@b.com'`（PostgreSQL 10+ で WAL 対応・クラッシュセーフになり明示作成可。実用上は B-Tree で十分なケースが多い。MySQL の InnoDB では Adaptive Hash Index として内部自動管理） |
| **GIN（Generalized Inverted Index）** | 配列、全文検索、JSONB | `WHERE tags @> '{go}'` |
| **GiST（Generalized Search Tree）** | 地理空間データ、範囲型 | `WHERE location <-> point(35.6, 139.7) < 1000` |

## 他の仕組みとどう関係するか

- **下位レイヤーとの関係:**
  - [[データ構造とアルゴリズム]] — B-Tree / [[ハッシュテーブル]]はデータ構造そのもの。[[計算量-BigO]]の理解がインデックスの性能評価に直結する
  - [[ファイルシステムとIO]] — インデックスはディスク上のページ（通常8KB）単位で読み書きされる。ランダムI/O vs シーケンシャルI/Oの違いがインデックス設計に影響する
  - [[メモリ管理]] — 頻繁にアクセスされるインデックスページはバッファプールにキャッシュされる。インデックスサイズがメモリに収まるかどうかが性能を大きく左右する

- **同レイヤーとの関係:**
  - [[RDB]] — インデックスは[[RDB]]のクエリ性能を決定づける最も重要な仕組み。正規化で分割されたテーブルのJOINにも不可欠
  - [[NoSQL]] — DynamoDBのパーティションキー/ソートキーやMongoDBのインデックスも、本質的には同じB-Treeベースの考え方
  - [[キャッシュ戦略]] — インデックスで十分に高速な場合、アプリケーション層のキャッシュが不要になることもある
  - [[マイグレーション]] — インデックスの追加・削除は[[マイグレーション]]で管理する。大きなテーブルへのインデックス追加は長時間ロックを取る可能性がある

- **上位レイヤーとの関係:**
  - [[Layer4-アプリケーション/_index|Layer 4: アプリケーション]] — ORMが生成するクエリがインデックスを使えているかは `EXPLAIN` で確認する
  - [[Layer5-パフォーマンス/_index|Layer 5: パフォーマンス・信頼性]] — データベースのパフォーマンスチューニングの中核がインデックス設計

```mermaid
graph LR
    subgraph "Layer 0-1"
        DS["データ構造<br/>(B-Tree)"]
        FS["ファイルシステム<br/>(ページI/O)"]
        MEM["メモリ管理<br/>(バッファプール)"]
    end

    subgraph "Layer 3"
        IDX["インデックス"]
        RDB["RDB"]
        CACHE["キャッシュ戦略"]
    end

    subgraph "Layer 4-5"
        APP["アプリケーション<br/>(EXPLAIN)"]
        PERF["パフォーマンス"]
    end

    DS --> IDX
    FS --> IDX
    MEM --> IDX
    IDX --> RDB
    IDX -.->|不要にできる| CACHE
    RDB --> APP
    IDX --> PERF
```

## 誤解されやすいポイント

### 1. 「インデックスを貼れば貼るほど速くなる」

インデックスは **読み取りを速くする代わりに書き込みを遅くする** トレードオフの仕組みである。INSERT / UPDATE / DELETE のたびに、テーブル本体に加えて全関連インデックスも更新する必要がある。

```
INSERT 1行 → テーブルに書き込み + インデックスA更新 + インデックスB更新 + インデックスC更新...
```

書き込みが多いテーブルにインデックスを10個も20個も作ると、INSERT性能が著しく劣化する。インデックスは「実際に発行されるクエリ」に基づいて必要最小限にする。

### 2. 「WHERE句にカラムがあれば自動的にインデックスが使われる」

以下のケースではインデックスが使われない（または非効率になる）:

```sql
-- ❌ カラムに関数を適用 → インデックスが効かない
SELECT * FROM users WHERE LOWER(email) = 'test@example.com';
-- ✅ 式インデックスを作るか、アプリ側で正規化して格納
CREATE INDEX idx_users_email_lower ON users (LOWER(email));

-- ❌ LIKE の前方一致以外 → B-Treeが使えない
SELECT * FROM users WHERE name LIKE '%田中%';
-- ✅ 前方一致ならB-Treeが使える
SELECT * FROM users WHERE name LIKE '田中%';

-- ❌ OR条件 → 各条件のインデックスが統合されにくい
SELECT * FROM users WHERE status = 'active' OR age > 30;

-- ❌ 型の不一致 → 暗黙の型変換でインデックスが効かない
SELECT * FROM users WHERE phone = 12345678; -- phone が VARCHAR の場合
```

### 3. 「主キーにインデックスを作る必要がある」

`PRIMARY KEY` 制約を設定すると、データベースが自動的にインデックスを作成する。手動で重複して作成するのは無駄である。同様に `UNIQUE` 制約も自動的にインデックスを作成する。

### 4. 「複合インデックスのカラム順序は関係ない」

複合インデックス（Composite Index）はカラムの順序が検索性能に直結する。B-Treeはインデックスの **左端のカラムからしか使えない**（最左プレフィックスルール）。

```sql
-- インデックス: (status, created_at)
CREATE INDEX idx_orders_status_created ON orders (status, created_at);

-- ✅ status だけの検索 → インデックスが使える
SELECT * FROM orders WHERE status = 'shipped';

-- ✅ status + created_at → インデックスが使える
SELECT * FROM orders WHERE status = 'shipped' AND created_at > '2026-01-01';

-- ❌ created_at だけの検索 → このインデックスは使えない
SELECT * FROM orders WHERE created_at > '2026-01-01';
```

### 5. 「カーディナリティが低いカラムにインデックスを貼っても意味がない」

boolean型のように値が2種類しかないカラムでもインデックスが有効なケースがある。全体の1%しかない `published = TRUE` を取得する場合、フルスキャンよりインデックススキャンの方が圧倒的に速い。こうした場面では **部分インデックス（Partial Index）** が特に有効。

```sql
-- 全行にインデックスを貼るのではなく、必要な条件だけ
CREATE INDEX idx_posts_published ON posts (created_at) WHERE published = TRUE;
```

## 設計のベストプラクティス

### 推奨パターン

1. **クエリ駆動で設計する**（[[インデックス設計の判断基準]]も参照）
   - テーブル設計時に「どんなクエリが来るか」を先に考え、そのクエリに対してインデックスを設計する
   - スロークエリログを有効にし、実際に遅いクエリを計測してからインデックスを追加する

2. **EXPLAIN を習慣にする**
   - 新しいクエリを書いたら必ず `EXPLAIN ANALYZE` でクエリプランを確認する
   - `Seq Scan`（全件走査）が大きなテーブルに出ていたら要注意

3. **複合インデックスの順序を最適化する**
   - 等値条件（`=`）のカラムを左に、範囲条件（`>`, `<`, `BETWEEN`）のカラムを右に配置する
   - `ORDER BY` で使うカラムも考慮する

4. **カバリングインデックス（Covering Index）を活用する**
   - クエリが必要とする全カラムがインデックスに含まれていれば、テーブル本体へのアクセスが不要になる（Index Only Scan）

```sql
-- posts から user_id と created_at だけ取得するクエリが頻出する場合
CREATE INDEX idx_posts_covering ON posts (user_id, created_at);

-- このクエリはテーブル本体を読まずにインデックスだけで完結する
SELECT user_id, created_at FROM posts WHERE user_id = 1 ORDER BY created_at DESC;
```

5. **部分インデックス（Partial Index）で無駄を削減する**
   - 全行ではなく条件を満たす行だけにインデックスを作る
   - インデックスサイズが小さくなり、書き込みコストも削減される

```sql
-- 未処理の注文だけにインデックス（大部分が処理済みならサイズが劇的に小さい）
CREATE INDEX idx_orders_pending ON orders (created_at) WHERE status = 'pending';
```

### アンチパターン

1. **「念のため」インデックス** — 使われないインデックスは書き込みコストとストレージを浪費するだけ。`pg_stat_user_indexes` で使用状況を監視する
2. **重複インデックス** — `(a)` と `(a, b)` の両方が存在する場合、`(a)` は不要（`(a, b)` が `(a)` の検索もカバーする）
3. **大規模テーブルへのインデックス追加を無計画に実行** — 通常の `CREATE INDEX` はテーブルロックを取る。PostgreSQLでは `CREATE INDEX CONCURRENTLY` を使う

## AIエージェントとの協働

> このトピックでAIコーディングエージェント（Claude Code, Copilot, Cursor 等）と協働するための観点。
> インデックスは「**実際のクエリパターンと書き込み頻度から逆算する設計判断**」であり、AI に「インデックスを貼って」と頼むと **「念のため全カラム」「JOIN カラム全部」など過剰提案** を出されやすい。AI には案を列挙させ、人間が **EXPLAIN と書き込み頻度** で採否を決める。

### AIに任せられる部分 / 人間が判断すべき部分

| タスク種類 | 任せ方（実装/レビュー） | 人間の関与 |
|---|---|---|
| 単純なクエリへのインデックス提案 | AI に複数案を出させる | テーブルサイズ・書き込み頻度・他クエリへの影響で採否を決定 |
| 複合インデックスのカラム順検討 | AI に「等値→範囲→ORDER BY」のルールで列挙させる | 実際の WHERE 句・SORT 順を提示し、最終的なカラム順は人間が決定 |
| 部分インデックス・カバリングインデックスの設計 | AI に候補を出させる | 対象データの分布（例: `status='pending'` が全体の何%か）を計測してから判断 |
| `EXPLAIN ANALYZE` 出力の解釈 | AI に分析と改善案を出させる | 開発DBと本番DBで統計が違うため、本番計測は必須 |
| マイグレーション SQL のロック影響評価 | AI に「ロックを取るか」のレビューをさせる | テーブルサイズと許容ダウンタイムから `CONCURRENTLY` を使うか判断 |
| 未使用インデックスの検出スクリプト | AI に任せる | 監視期間（最低数週間）と除外条件（バックアップ用など）は人間が指定 |

### AI生成コードのレビュー観点（このトピック固有）

AI生成のインデックス定義・クエリを受け取ったとき、最低限ここを見る。

1. **「念のため」インデックスになっていないか** — 実際のクエリと結びついていない・カーディナリティが極端に低い・書き込み頻度の高いテーブルへの過剰な追加。`pg_stat_user_indexes.idx_scan = 0` で検出可能
2. **複合インデックスのカラム順が「等値→範囲→ORDER BY」に従っているか** — 等値条件カラムを左、範囲条件カラムを右に配置。逆になっていると最左プレフィックスルールでインデックスが部分的にしか効かない
3. **マイグレーションがロックを引き起こさないか** — 本番テーブルへの `CREATE INDEX` は PostgreSQL では `CONCURRENTLY`、MySQL では `ALGORITHM=INPLACE, LOCK=NONE` を指定しないと長時間テーブルロックを取る。AI 生成のマイグレーションには明示確認が必要

### 効くプロンプトの型

このトピックに関する実装を AI に依頼するとき、コンテキストとして渡すべき情報・制約・成功基準のテンプレ。

```
# 前提（プロジェクトの状況・既存のコード規約）
- DB: PostgreSQL 16（または MySQL 8.4 InnoDB）
- 対象テーブル定義と現在のインデックス: [...]
- テーブル行数: 約 {N}行 (1日 +{M}行 増加)
- 想定される代表的なクエリ（WHERE, ORDER BY, LIMIT 含めて記述）:
  1. SELECT ... WHERE status = ? AND created_at > ? ORDER BY created_at DESC LIMIT 20
  2. SELECT ... WHERE user_id = ?
- 書き込み頻度: 1秒あたり {K}回
- 既存の遅いクエリ（あれば EXPLAIN ANALYZE 結果を貼る）

# やってほしいこと
- 上記クエリを高速化するインデックス設計を提案
- 各インデックス追加の理由・想定効果・書き込みコスト影響を併記
- 既存インデックスとの重複・冗長があれば指摘

# 守ってほしい制約
- 「念のため」のインデックスを追加しない（理由を必ず明示）
- 複合インデックスは等値→範囲→ORDER BY の順
- 本番マイグレーションは `CREATE INDEX CONCURRENTLY` を使う
- 主キーや UNIQUE 制約と重複するインデックスを作らない

# 完了の判断基準
- 各クエリの想定 EXPLAIN プランで Seq Scan が大きなテーブルに出ない
- 追加するインデックス数を最小化（複合・部分・カバリングで集約）
- マイグレーションで本番テーブルロックが発生しない
```

### AI実装のアンチパターン

LLM 生成コードで頻出する過剰設計・誤用パターン。レビュー時の照合表として使う。

| アンチパターン | なぜ問題か | レビュー観点 / 対策 |
|---|---|---|
| 全カラムに個別インデックスを作成 | 書き込み性能が劣化、ストレージ浪費、プランナが選択に迷う | 実際のクエリを分析し、複合インデックスで集約する |
| マイグレーションで `CREATE INDEX` を無条件に使用 | 本番環境で長時間テーブルロックが発生 | PostgreSQL は `CREATE INDEX CONCURRENTLY`、MySQL は `ALGORITHM=INPLACE, LOCK=NONE` を使う |
| 全 JOIN カラムにインデックス付与 | 小さなテーブル同士の JOIN ではオーバーヘッドの方が大きい | テーブルサイズとクエリ頻度を考慮して判断 |
| `SELECT *` でカバリングインデックスを無効化 | Index Only Scan が使えず、必ずテーブル本体にアクセス | 必要カラムだけを明示指定 |
| `WHERE LOWER(email) = ?` のように関数を適用 | 通常のインデックスが効かない | 式インデックス `(LOWER(email))` を作るか、アプリ側で正規化して格納 |
| 複合インデックスのカラム順を「使用順」で決める | 最左プレフィックスルールに反する組み合わせができる | 等値→範囲→ORDER BY の順で構成 |
| カーディナリティ低いカラムにインデックス | boolean 等は基本的に効果薄 | 低頻度値のみ取得するなら部分インデックスを検討 |

→ レイヤー横断のアンチパターン索引: [[_anti-patterns/_index|AIアンチパターン索引]] / [[_anti-patterns/レイヤー別/Layer3|Layer 3 アンチパターン集]]

## 具体例

### インデックス設計の実践（PostgreSQL）

```sql
-- サンプルテーブル
CREATE TABLE orders (
    id BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    user_id BIGINT NOT NULL REFERENCES users(id),
    status VARCHAR(20) NOT NULL DEFAULT 'pending',
    total_amount DECIMAL(10,2) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

-- 1. 外部キーには必ずインデックス（JOINとカスケード削除の性能に影響）
CREATE INDEX idx_orders_user_id ON orders (user_id);

-- 2. 複合インデックス: ステータス別の最新注文を取得するクエリ用
--    等値条件(status)を左、範囲条件(created_at)を右に配置
CREATE INDEX idx_orders_status_created ON orders (status, created_at DESC);

-- このクエリに最適:
-- SELECT * FROM orders WHERE status = 'shipped' ORDER BY created_at DESC LIMIT 20;

-- 3. 部分インデックス: 未処理注文だけ（全体の5%程度を想定）
CREATE INDEX idx_orders_pending ON orders (created_at)
    WHERE status = 'pending';

-- このクエリに最適:
-- SELECT * FROM orders WHERE status = 'pending' ORDER BY created_at;
```

### EXPLAIN ANALYZE の読み方

```sql
EXPLAIN ANALYZE
SELECT * FROM orders
WHERE status = 'shipped' AND created_at > '2026-01-01'
ORDER BY created_at DESC
LIMIT 20;
```

```
-- 出力例（インデックスが効いている場合）:
Limit  (cost=0.42..1.23 rows=20 width=48) (actual time=0.032..0.045 rows=20 loops=1)
  ->  Index Scan Backward using idx_orders_status_created on orders
        (cost=0.42..4521.33 rows=112000 width=48) (actual time=0.031..0.042 rows=20 loops=1)
        Index Cond: ((status = 'shipped') AND (created_at > '2026-01-01'))
Planning Time: 0.152 ms
Execution Time: 0.061 ms
```

読むべきポイント:

| 項目 | 意味 | 注目すべき値 |
|------|------|-------------|
| `Index Scan` / `Index Only Scan` | インデックスを使っている | 良好 |
| `Seq Scan` | 全件走査 | 大きなテーブルなら要改善 |
| `actual time` | 実際の実行時間（ms） | 目標レスポンスタイムと比較 |
| `rows` | 実際に処理された行数 | 見積もり(`cost`の`rows`)と大きく乖離していないか |
| `Bitmap Index Scan` | インデックスでビットマップを作り、テーブルをまとめて読む | 中間的な効率 |

### 未使用インデックスの検出（PostgreSQL）

```sql
-- 使われていないインデックスを検出する
SELECT
    schemaname,
    tablename,
    indexname,
    idx_scan AS times_used,
    pg_size_pretty(pg_relation_size(indexrelid)) AS index_size
FROM pg_stat_user_indexes
WHERE idx_scan = 0          -- 一度も使われていない
  AND indexrelname NOT LIKE '%_pkey'  -- 主キーは除外
  AND indexrelname NOT LIKE '%_unique%'  -- UNIQUE制約は除外
ORDER BY pg_relation_size(indexrelid) DESC;
```

### アプリケーションコードからの確認（TypeScript）

```typescript
// 開発環境でスロークエリを検出するミドルウェア例
import { Pool, type QueryResult } from "pg";

const pool = new Pool({ connectionString: process.env.DATABASE_URL });

async function queryWithExplain<T>(
  sql: string,
  params: unknown[] = []
): Promise<QueryResult<T>> {
  // 開発環境でのみ EXPLAIN ANALYZE を実行
  if (process.env.NODE_ENV === "development") {
    const explained = await pool.query(`EXPLAIN ANALYZE ${sql}`, params);
    const plan = explained.rows.map((r) => Object.values(r)[0]).join("\n");

    // Seq Scan が大きなテーブルに対して出ていたら警告
    if (plan.includes("Seq Scan") && !plan.includes("rows=0")) {
      console.warn(`[SLOW QUERY WARNING] Seq Scan detected:\n${sql}\n${plan}`);
    }
  }

  return pool.query<T>(sql, params);
}

// 使用例
const result = await queryWithExplain(
  "SELECT * FROM orders WHERE status = $1 ORDER BY created_at DESC LIMIT $2",
  ["shipped", 20]
);
```

### Go — クエリ実行とインデックスの確認

```go
package main

import (
	"context"
	"database/sql"
	"fmt"
	"log"
	"strings"

	_ "github.com/lib/pq"
)

func main() {
	db, err := sql.Open("postgres", "postgres://localhost/mydb?sslmode=disable")
	if err != nil {
		log.Fatal(err)
	}
	defer db.Close()

	// インデックスが効くクエリ
	rows, err := db.QueryContext(context.Background(),
		`SELECT id, total_amount, created_at
		 FROM orders
		 WHERE status = $1 AND created_at > $2
		 ORDER BY created_at DESC
		 LIMIT $3`,
		"shipped", "2026-01-01", 20,
	)
	if err != nil {
		log.Fatal(err)
	}
	defer rows.Close()

	for rows.Next() {
		var id int64
		var amount float64
		var createdAt string
		if err := rows.Scan(&id, &amount, &createdAt); err != nil {
			log.Fatal(err)
		}
		fmt.Printf("Order %d: ¥%.0f (%s)\n", id, amount, createdAt)
	}

	// 開発時: EXPLAIN で確認
	explainQuery := `EXPLAIN ANALYZE
		SELECT id, total_amount, created_at
		FROM orders
		WHERE status = $1 AND created_at > $2
		ORDER BY created_at DESC
		LIMIT $3`

	explainRows, err := db.QueryContext(context.Background(),
		explainQuery, "shipped", "2026-01-01", 20,
	)
	if err != nil {
		log.Fatal(err)
	}
	defer explainRows.Close()

	fmt.Println("\n--- Query Plan ---")
	for explainRows.Next() {
		var line string
		if err := explainRows.Scan(&line); err != nil {
			log.Fatal(err)
		}
		fmt.Println(line)
		if strings.Contains(line, "Seq Scan") {
			fmt.Println("⚠ WARNING: Sequential scan detected!")
		}
	}
}
```

## 参考リソース

- **記事**: [Use The Index, Luke](https://use-the-index-luke.com/) — SQLインデックスに特化した無料の解説サイト（最もおすすめ）
- **書籍**: 『達人に学ぶDB設計 徹底指南書』（ミック著） — インデックス設計の実践を含むDB設計の包括的ガイド
- **書籍**: 『SQLパフォーマンス詳解』（Markus Winand著） — Use The Index, Luke の書籍版、B-Treeの内部構造から解説
- **公式ドキュメント**: [PostgreSQL - Indexes](https://www.postgresql.org/docs/current/indexes.html) — 各インデックス種別の詳細
- **公式ドキュメント**: [MySQL - Optimization and Indexes](https://dev.mysql.com/doc/refman/8.4/en/optimization-indexes.html) — MySQL固有のインデックス最適化

## 理解度セルフチェック

> 答えられなければ本文に戻る。答えはこのファイル内に必ずある。

1. **「インデックスを増やせば増やすほど速くなる」が誤りである理由を、書き込みパスに何が起こるかを示しながら30秒で説明せよ。**
2. **次のテーブルに `CREATE INDEX idx ON orders (status, created_at);` のインデックスがある。以下4つのクエリのうち、このインデックスが「効く」ものはどれか。** 効かないものについてはその理由も述べよ。
    - (a) `SELECT * FROM orders WHERE status = 'shipped';`
    - (b) `SELECT * FROM orders WHERE status = 'shipped' AND created_at > '2026-01-01';`
    - (c) `SELECT * FROM orders WHERE created_at > '2026-01-01';`
    - (d) `SELECT * FROM orders WHERE LOWER(status) = 'shipped';`
3. **AI生成コードレビュー設問:** AI が以下のマイグレーション SQL を生成した。本番テーブル（行数 5000万、書き込み 100 req/s）に対してこのまま適用するときの **問題点を3つ以上** 指摘し、修正案を示せ。

```sql
-- migration_20260504_add_indexes.sql
CREATE INDEX idx_users_email      ON users (email);
CREATE INDEX idx_users_name       ON users (name);
CREATE INDEX idx_users_phone      ON users (phone);
CREATE INDEX idx_users_created_at ON users (created_at);
CREATE INDEX idx_users_updated_at ON users (updated_at);

-- ユーザーステータス別の最新一覧取得用
CREATE INDEX idx_users_status_created ON users (created_at, status);
```

> [!note]- 解答の指針
>
> > [!info] 用語ミニ辞典
> > - **B+Tree:** リーフノードにのみデータを格納し、リーフ間を双方向リンクで繋いだ B-Tree の改良版。RDB のインデックスはほぼ全て B+Tree。範囲検索でリーフを横断するだけで済む（詳細: [[B-TreeとB+Tree]]）
> > - **ノード分割 (Page Split):** B+Tree のノードがキーで満杯になったとき、新しいノードを作って中央キーを親に押し上げる操作。ランダム値の挿入で頻発するとディスク I/O が増え書き込みが遅くなる
> > - **テーブルロック:** スキーマ変更中にテーブル全体を排他ロックする操作。読み書きを停止させ、本番ではダウンタイムにつながる
> > - **最左プレフィックスルール:** 複合インデックスは「左から順に連続してマッチする部分」しかインデックスとして使えないという B-Tree の構造上の制約
> > - **`CONCURRENTLY` (PostgreSQL):** インデックス構築中にテーブルへの書き込みを許す。代わりに2回スキャンが走るため通常より時間はかかる
> > - **インデックス INVALID 状態:** `CREATE INDEX CONCURRENTLY` が失敗したとき、不完全なインデックスがクエリに使われない状態で残る現象。`pg_indexes` で `indisvalid = false` を確認し、`DROP INDEX CONCURRENTLY` でクリーンアップする
>
> 1. インデックスはテーブル本体とは別の B+Tree を持つ。INSERT/UPDATE/DELETE のたびに **本体 + 全関連インデックスを更新** する必要があり、インデックス数に比例して書き込みコストが増える。さらに B+Tree の更新はノード分割やページ移動を引き起こすため、ストレージ I/O も追加で発生する。「読み取りを速くする代わりに書き込みを遅くする」のがインデックスの本質
> 2. 効くもの: **(a)、(b)**。効かないもの: **(c)、(d)**
>     - **(a) 効く** — 複合インデックスの左端カラム `status` 単独条件は使える
>     - **(b) 効く** — 等値 `status = ?` で範囲を絞り、続く `created_at` で範囲検索。複合インデックスの理想的な使い方
>     - **(c) 効かない** — `status` を飛ばして `created_at` だけの条件は最左プレフィックスルールに反する。`(created_at)` 単独インデックスが必要
>     - **(d) 効かない** — `LOWER(status)` のように関数でカラムを包むとインデックスが使えない。`(LOWER(status))` の式インデックスを作るか、アプリ側で小文字化して保存
> 3. AI生成マイグレーションの問題点（最低限以下を指摘できれば本文を理解している）:
>     - **`CONCURRENTLY` がない** — 5000万行のテーブルに通常の `CREATE INDEX` を発行すると、インデックス構築完了まで数十分〜数時間テーブルロックを取る。100 req/s の書き込みが全停止し、ダウンタイムになる。修正: `CREATE INDEX CONCURRENTLY ...`（注: `CONCURRENTLY` は単一トランザクション内で実行不可なので、マイグレーションフレームワークでも個別実行する設定にする）
>     - **`idx_users_email` が無駄になる可能性** — `email` に UNIQUE 制約がある前提なら自動でインデックスが作られているため重複作成になる。前提を確認するために `\d users` (psql) や `pg_indexes` で既存インデックスをチェックすべき。逆に UNIQUE 制約がないなら、まずそれを付ける設計判断が先にあるべき
>     - **`idx_users_status_created` のカラム順が逆** — 「ステータス別の最新一覧」用なら **`(status, created_at)`** が正しい。`(created_at, status)` だと `WHERE status = ? ORDER BY created_at` で左端の `created_at` を使い切れず非効率。等値→範囲→ORDER BY のルールに従う
>     - **`idx_users_updated_at` の正当性が不明** — `updated_at` で検索することは実務ではほぼない。本当にクエリパターンが存在するか確認、なければ削除
>     - **5本同時追加のリスクとロールバック設計の欠落** — 各 `CREATE INDEX CONCURRENTLY` は失敗時にインデックスが INVALID 状態で残る。失敗時のクリーンアップ手順も明記すべき
>     - **書き込みコスト試算がない** — インデックスを5本追加すると INSERT/UPDATE のたびに5本分の B+Tree 更新が走る。書き込み 100 req/s のテーブルに5本追加するのは慎重に。本当に全部必要か、`pg_stat_user_indexes` での将来的な使用率監視計画とセットで提案すべき

## 学習メモ

- [[データ構造とアルゴリズム]]のB-Treeと[[ハッシュテーブル]]を先に理解すると、インデックスの「なぜ」が腑に落ちる
- [[ファイルシステムとIO]]のページ単位I/Oの知識が、B-Treeのノードサイズが重要な理由を説明する
- [[RDB]]のEXPLAIN出力を読む習慣をつけるのが、インデックス設計の最短経路
- [[サロゲートキーと自然キー]]でUUID v4がインデックス性能に悪影響を与える理由も、B-Treeの構造から理解できる
